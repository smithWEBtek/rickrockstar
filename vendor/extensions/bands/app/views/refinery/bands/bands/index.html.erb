<% content_for :body do %>
  <ul id="bands">
    <% @bands.each do |band| %>
      <li>
        <%= link_to band, refinery.bands_band_path(band) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
