<%= render 'refinery/admin/search_header', :url => refinery.bands_admin_bands_path %>
<div class='pagination_container'>
  <% if @bands.any? %>
    <%= render 'bands' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
