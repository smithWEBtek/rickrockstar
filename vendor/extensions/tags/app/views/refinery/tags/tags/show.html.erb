<% content_for :body_content_title do %>
  <%= @tag.name %>
<% end %>

<% content_for :body do %>
  <section>
    <h1>Name</h1>
    <p>
      <%=raw @tag.name %>
    </p>
  </section>
<% end %>

<% content_for :side_body do %>
  <aside>
    <h2><%= t('.other') %></h2>
    <ul id="tags">
      <% @tags.each do |tag| %>
        <li>
          <%= link_to tag.name, refinery.tags_tag_path(tag) %>
        </li>
      <% end %>
    </ul>
  </aside>
<% end %>

<%= render '/refinery/content_page' %>
